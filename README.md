# guptacommunity

## Backend

The backend is a Spring Boot application in `backend/`.

Run it from the repository root with:

```text
cd backend
mvn spring-boot:run
```

The health endpoint is available at `http://localhost:8080/api/health`.